# 2026-03-09

- Continued the EVHOME / Lark Base / Lark Calendar automation work from 2026-03-08.
- Lark Calendar user-token flow is working against the correct shared calendar name: `MOMO 施工日历`.
- The correct Lark OAuth user-token flow uses `authen/v1/access_token` with `app_id` and `app_secret` (not `client_id` / `client_secret`), and `authen/v1/refresh_access_token` for refresh.
- Local runtime credential files were introduced so daily automation does not have to depend on live 1Password access for every run.
- Lark calendar sync was corrected to:
  - write to `MOMO 施工日历`,
  - use single-day all-day events,
  - write address into the real `location` field,
  - delete old incorrect timed / multi-day duplicates,
  - and refresh user access tokens from local runtime secrets before scheduled sync runs.
- Launchd jobs are installed for:
  - `com.openclaw.evhome-larkbase-sync` (09:00 / 17:00)
  - `com.openclaw.lark-calendar-user-sync` (09:00 / 17:00)
- EVHOME flows were refactored to prefer local runtime credentials over 1Password:
  - new shared loader `scripts/evhome_credentials.js`
  - `evhome_export_all.js` and `evhome_export_paid.js` now prefer env / local runtime file first, then 1Password fallback.
- Default EVHOME local credential file path is `/Users/yixiao/.openclaw/workspace/secret/evhome_runtime.json` with JSON shape `{ "username": "...", "password": "..." }`.
- OCR pipeline status:
  - source field: `补贴获批邮件截图`
  - OCR output field: `EVHOME_PREQUAL_ID_OCR`
  - desired writebacks from EVHOME portal draft flow: `客户地址` and `CRH_NO`.
- Local OCR on macOS now works using `scripts/ocr_prequal.swift` (Vision framework), without needing a cloud OCR call for routine runs.
- OCR audit/report artifacts created:
  - `scripts/lark_audit_prequal_ocr.js`
  - `reports/lark_prequal_ocr_audit.md`
  - `reports/lark_prequal_ocr_audit.json`
  - evidence images in `output/ocr_audit_samples/`
- OCR audit summary at handoff time:
  - total audited: 27
  - high-confidence: 4
  - suspicious: 22
  - unusable/non-prequal: 1
- Important OCR review rule from user:
  - valid EVHOME prequalification IDs should be 13 characters
  - OCR sometimes appends a stray trailing `P`
  - future logic should support two OCR candidates for ambiguous cases (especially `0/O` and trailing `P`) instead of forcing a single guess.
- Explicit OCR corrections identified in review:
  - `recfwaBuXJ`: `APB94415E9401P` should likely be `APB94415E9401`
  - `recIFyufPQ`: `AVS1409JG1369P` should likely be `AVS1409JG1369`
- One unusable/non-prequal screenshot case identified:
  - `recsbzWEk0` contains portal/resubmission screenshots and `CRH-042202`, not true prequalification email evidence.
- Constructed backlog helper and backfill work completed:
  - helper script `scripts/lark_list_constructed_pending.js`
  - output files `output/lark_constructed_pending.json` and `output/lark_constructed_pending_stdout.json`
  - backfill script `scripts/lark_backfill_constructed_crh.js`
  - output `output/lark_constructed_backfill_results.json`
- Four constructed records were backfilled from existing Lark evidence (not fresh portal draft creation) and backlog reduced to zero:
  - `recv6eY0PNDWST` → `CRH-039184`
  - `recfwaBuXJ` → `CRH-039269`
  - `rect0coSCz` → `CRH-039270`
  - `recQyvz42K` → `CRH-041589`
- Important unresolved blocker at handoff time:
  - full EVHOME portal draft-creation automation is still not fully reliable,
  - because stable EVHOME authenticated browser state / credential usage needs further hardening,
  - and OCR should be upgraded to dual-candidate review before using OCR results blindly in portal claims.
- Current best next step after handoff:
  1. implement dual-candidate OCR review/writeback (`EVHOME_PREQUAL_ID_OCR` + alternate candidate, likely a new field),
  2. use only high-confidence or manually reviewed OCR values for portal draft creation,
  3. continue hardening EVHOME local-credential login flow.
- Relevant commits during this phase:
  - `cf096c6` — Add scheduled evhome sync and harden calendar sync
  - `daf1b6a` — Finalize local credential refresh flow for calendar sync
  - `02faa66` — Improve EVHOME OCR writeback pipeline
  - `2a0071f` — Backfill constructed EVHOME claim IDs from Lark evidence
  - `7694d76` — Harden EVHOME exports for tmux-backed 1Password auth
  - `dd275c4` — Prefer local runtime creds for EVHOME flows
  - `3ccafe4` — Add Lark prequal OCR audit report
- Continued in a fresh session later the same evening.
- Added reviewed-prequal writeback flow:
  - new script `scripts/lark_writeback_prequal_reviewed.js`
  - wrote reviewed/final prequalification IDs into Lark field `补贴获批ID` for 26 usable OCR-audited records
  - corrected two raw OCR field values with stray trailing `P`:
    - `recfwaBuXJ` → `APB94415E9401`
    - `recIFyufPQ` → `AVS1409JG1369`
  - fixed stale reviewed-field mismatch for `recv6gcxT3WdAf` to `AUS7814MN7774`
  - filled missing reviewed ID for `recCmXmzYk` as `AD52905JS2865`
  - left `recsbzWEk0` blank for prequal ID because it is not valid prequalification evidence
- Confirmed OCR implementation detail for future reference:
  - OCR is performed locally on macOS using `scripts/ocr_prequal.swift` and the Vision framework, not a cloud OCR/model call for routine runs.
- Explored EVHOME draft-creation flow in the portal and confirmed:
  - `New Application` → verify prequalification ID → `Start Application` opens `termsandconditions?appid=...`
  - after accepting terms and hitting `Save and Continue`, the flow reaches required documents
  - `Save and Exit` at that point is enough to create a dashboard draft / started application with a `CRH-*` application ID.
- One draft was created during exploration before the user tightened the workflow:
  - address: `10800 Sonora Ave, Rancho Cucamonga, CA 91701`
  - created EVHOME application: `CRH-040208`
  - dashboard status observed: `Started`
- User corrected the business rule after this exploration:
  - do not bulk draft/claim everything with a reviewed prequalification ID
  - draft claim is allowed only when `施工日期` is earlier than the current date
  - if `施工日期` is empty, draft claim is not allowed
  - customer-progress text is not the hard gate; `施工日期` is the hard gate.
- User then changed workflow again and paused draft-claim work entirely:
  - for now keep only the OCR capability
  - stop doing EVHOME draft-claim creation
  - later, after AHJ inspection is complete, the user will send:
    - all inspection photos
    - city permit with final signature
    - customer invoice
  - only then should the assistant directly execute the EVHOME claim + upload flow.
- User sent 5 inspection/installation photos for `14021 Fernview St` and said inspection passed / materials can be uploaded.
- Current requirement for that address before claim+upload should proceed:
  - still need the city permit with final signature
  - and the customer invoice.
