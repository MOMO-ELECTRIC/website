# 2026-03-10

- Continued the EVHOME prequalification OCR task in a fresh session.
- Checked prior context from `memory/2026-03-09.md`; long-term `MEMORY.md` was not present in workspace at session start.
- Confirmed the Lark Base table already has both fields:
  - raw OCR field: `EVHOME_PREQUAL_ID_OCR`
  - reviewed/final field: `补贴获批ID`
- Added `scripts/lark_writeback_prequal_reviewed.js` to write reviewed prequalification IDs from `output/lark_prequal_ocr_audit.json` back into Lark Base.
- Chosen data model for now:
  - keep `EVHOME_PREQUAL_ID_OCR` as the machine OCR/raw field,
  - use `补贴获批ID` as the reviewed/final usable prequalification ID field.
- Ran writeback for all usable audited prequalification records:
  - total candidates written to `补贴获批ID`: 26
  - unusable/non-prequal skipped: 1 (`recsbzWEk0`)
- Also corrected two obvious OCR raw-field errors where OCR had an extra trailing `P`:
  - `recfwaBuXJ`: `APB94415E9401P` -> `APB94415E9401`
  - `recIFyufPQ`: `AVS1409JG1369P` -> `AVS1409JG1369`
- Fixed one stale reviewed field mismatch:
  - `recv6gcxT3WdAf` reviewed field now matches correct OCR/reviewed ID `AUS7814MN7774`
- Filled one missing reviewed ID even though raw OCR field remains blank:
  - `recCmXmzYk` -> reviewed/final `AD52905JS2865`
- Post-write spot checks confirmed:
  - `recv6gcxT3WdAf` OCR/reviewed both `AUS7814MN7774`
  - `recfwaBuXJ` OCR/reviewed both `APB94415E9401`
  - `recIFyufPQ` OCR/reviewed both `AVS1409JG1369`
  - `recCmXmzYk` reviewed `AD52905JS2865`
  - `recsbzWEk0` remains blank for prequal ID because it is not valid prequalification evidence.
- Output artifact from this pass: `output/lark_prequal_reviewed_writeback.json`
- User clarified workflow change after draft-claim exploration:
  - for now, keep only the OCR capability,
  - stop doing EVHOME draft claim creation,
  - later, after AHJ inspection is complete, the user will send the full claim package:
    - all inspection photos,
    - city permit with final signature,
    - and the customer invoice,
  - and only then should the assistant execute the EVHOME claim + upload flow.
